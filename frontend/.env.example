# ---- Frontend environment template ----
# Vite inlines VITE_* vars at BUILD time. Copy to .env for local dev.

# Dev points at the local Django server:
VITE_API_BASE_URL=http://localhost:8000/api

# Optional Spline scene URL (unused by the current 3D hero).
VITE_SPLINE_SCENE_URL=
